/**
 * Enumeration of item types in the game.
 * Defines the possible effects items can have on players.
 */
public enum ItemType {
    /** Increases the player's lives when collected. */
    LIFE_BOOST,
    /** Decreases the player's lives when encountered. */
    PENALTY
}
